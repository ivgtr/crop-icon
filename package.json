{
  "name": "crop-icon",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ivgtr/crop-icon.git"
  },
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "npm run build && node scripts/dev.mjs",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run typecheck && node --check public/app.js && npm test"
  },
  "dependencies": {
    "image-size": "2.0.2"
  },
  "devDependencies": {
    "@types/node": "22.19.7",
    "typescript": "5.8.3"
  }
}
